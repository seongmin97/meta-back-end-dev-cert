https://github.com/seongmin97/meta-assessment


## Starting the project
- env
- git repo
- static content
- URLs and Routes

## Project functionality
- Working with databases and models
- Serialization involves converting the model instances to native Python datatypes so that they can be rendered into JSON format. 
- Deserialization parses the data back into the model instance after first validating the incoming data.


## Security and testing
- DRF authentication
  - Remote user
    - singed against username and password
  - session
  - token-based
    - client-server platform
    - a string of random characters
    - @permission_class[]
- testing
